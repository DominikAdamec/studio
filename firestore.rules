rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Users can only create, read, and update their own user document.
    match /users/{userId} {
      allow read, update, create: if request.auth.uid == userId;
    }
  }
}
